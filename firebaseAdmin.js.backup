const admin = require("firebase-admin");
const serviceAccount = require("./location-maison-476fb-firebase-adminsdk-fbsvc-680e2079e2.json");

admin.initializeApp({
  credential: admin.credential.cert(serviceAccount)
});

const db = admin.firestore();

module.exports = db;
